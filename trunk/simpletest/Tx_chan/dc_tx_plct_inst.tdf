(
	q_sig[],
	cout_sig
) = dc_tx_plct	(
	.clock = clock_sig,
	.clk_en = clk_en_sig,
	.cnt_en = cnt_en_sig,
	.cin = cin_sig,
	.sload = sload_sig,
	.aclr = aclr_sig,
	.data[] = data_sig[]
)
RETURNS (
	.q[],
	.cout
);
