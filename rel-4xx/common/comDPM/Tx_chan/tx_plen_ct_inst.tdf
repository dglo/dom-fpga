(
	q_sig[],
	cout_sig
) = tx_plen_ct	(
	.clock = clock_sig,
	.clk_en = clk_en_sig,
	.cnt_en = cnt_en_sig,
	.sload = sload_sig,
	.aclr = aclr_sig,
	.data[] = data_sig[]
)
RETURNS (
	.q[],
	.cout
);
